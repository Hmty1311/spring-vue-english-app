spring:
  application:
    name: eng-app

  datasource:
    url: jdbc:postgresql://localhost:5433/engapp
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  port: 8080